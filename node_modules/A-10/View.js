module.exports=class View{
	constructor(res,viewPath,sendData){

		try{
			fs.statSync(viewPath);

			var html=fs.readFileSync(viewPath);
			res.write(html);

		}catch(err){
			console.log(err);
			res.write("<p>ERROR : Not found View File '"+viewPath+"'.</p>");
		}
	}
};